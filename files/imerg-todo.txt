1. check for 0 size files and delete them at the end of the ETL
2. file sizes are diff in temp extract and E:/globlimerg
3. run addrasterstomosaic only if tif>0
4. could not get dates-check records with no date and remove